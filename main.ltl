// ltl-prolog-lite/main.ltl
//
// Tiny Prolog interpreter with unification and DCG.
//
// This is an early skeleton — contribution is welcome! See the
// README for build instructions.

import std::io
import std::fmt

pub fn main(args: [str]) -> Int {
    io::println("ltl-prolog-lite v0.1.0")
    io::println("Tiny Prolog interpreter with unification and DCG.")
    0
}
