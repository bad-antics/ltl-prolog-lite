// examples/demo.ltl — runnable demo for ltl_prolog_lite

import std::io
use ltl_prolog_lite as lib

fn main() {
    io::println("ltl_prolog_lite demo starting...")
    let result = lib::main() |> io::inspect()
    io::println("demo complete")
}